SELECT 
    COLUMN_NAME,
    DATA_TYPE,
    IS_NULLABLE,
    CHARACTER_MAXIMUM_LENGTH
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'Posts'
ORDER BY ORDINAL_POSITION;
GO

EXEC sp_help 'Posts';
GO

SElECT * FROM Posts;
go

SELECT name 
FROM sys.tables
ORDER BY name;
GO

SELECT 
    TABLE_NAME,
    COLUMN_NAME,
    DATA_TYPE
FROM INFORMATION_SCHEMA.COLUMNS
ORDER BY TABLE_NAME;
GO

ALTER TABLE Users
ADD AvatarUrl NVARCHAR(500) NULL;
GO

ALTER TABLE Posts
ADD Slug NVARCHAR(300) NULL;
GO

SELECT COUNT(*)
FROM Comments
WHERE PostId = @postId
AND IsDeleted = 0
GO